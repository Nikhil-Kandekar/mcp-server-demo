[project]
name = "mcp-server-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.14.0",
    "nest-asyncio>=1.6.0",
    "playwright>=1.55.0",
    "psutil>=7.0.0",
]
